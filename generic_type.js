"use strict";
// Generic type - This is a way of auto generating data return base on the data passed by the user
function Say(text) {
    // so the parameter will be assigned to a data type base of the passed data
    console.log(text);
}

Say('Hello world')


// generic class